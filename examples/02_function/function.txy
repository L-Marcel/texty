proc swap(array: int[]; a, b: int)
	var temp: int = array[a];
  array[a] = array[b];
  array[b] = temp;
endproc;

fn partition(array: int[]; min, max: int): int
	var i: int = (min - 1);
	var pivot: int = i + 1;
	swap(array, pivot, max);
endfn;