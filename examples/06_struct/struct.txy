struct Address
  street, city, country: string;
endstruct;

struct User
  id: long;
  age: int;
  name, password, bio: string;
  admin: bool;
  height, weight: float;
  balance: double;
  gender: char;
  version: int;
  permission: byte;
  supervisor: option<User>;
  address: Address;
endstruct;