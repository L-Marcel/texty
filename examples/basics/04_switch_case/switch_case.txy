proc main()
  for (i in 0..8)
    switch i
      case 0: 
        println("Zero!");
        break;
      case 1, 2, 3:
        println("Um, dois ou tres!");
      default: 
        println("Diferente de zero, um, dois e tres!");
    endswitch;
  endfor;
endproc;