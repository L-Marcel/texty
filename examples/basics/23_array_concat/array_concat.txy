proc main()
    var first: int[] = new int[]{1};
    var second: int[] = new int[]{4, 1, 6};
    var result: int[] = first << second;
    delete(&first);
    delete(&second);
    println(string(result));
endproc;
